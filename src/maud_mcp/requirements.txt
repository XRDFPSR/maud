# MAUD MCP 依赖
# 核心运行不需要 mcp 包，只需要 JDK + Maud.jar
# MCP Server 需要额外安装: pip install mcp

# 核心（最小运行时）
# 无额外 Python 依赖 — 只用标准库

# MCP Server (可选)
mcp>=1.0.0

# REST API (可选)
# fastapi>=0.100.0
# uvicorn>=0.30.0

# 开发/测试
pytest>=7.0.0
